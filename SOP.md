# P30 Agent Financial OS: 量化交易标准作业程序 (SOP v1.0)

本系统作为一个量化交易操作系统，其核心价值在于将复杂的交易行为转化为标准化、自动化的流水线（Pipeline）。

---

## 阶段 1：情报感知与发现 (Discovery)
**[执行主体: Intelligence Agent & Shadow Agent]**
1. **巨鲸异动监控 (Priority)**: 实时监控高胜率钱包（如 RN1, swisstony）的链上/CLOB 下单行为。
2. **多源情报扫描**: 利用 OpenBB 和 Firecrawl 监控突发新闻、推特关键字。
3. **信号对齐**: 当“巨鲸动作”与“突发新闻”在同一市场发生重叠时，自动提升该事件的优先级为“极高”。

## 阶段 2：深度分析与定价 (Analysis)
**[执行主体: Logic Arb Agent & Shadow Agent (Powered by MiroFish)]**
1. **群体智能模拟 (MiroFish)**: 通过模拟数千个 Agent 在当前情报背景下的博弈，推导出事件的真实概率轨迹。
2. **逻辑反推**: 针对阶段 1 发现的巨鲸动作，利用反事实推理分析其买入的背后逻辑是否站得住脚。
3. **数学建模**: 调用 NegRisk 算法模型，计算互斥选项间的数学均衡点。
4. **期望值计算**: 计算 EV (Expected Value)，仅当 EV > 阈值时进入下一阶段。

## 阶段 3：风控与约束 (Risk Control)
**[执行主体: Risk Agent]**
1. **头寸分配**: 基于凯利公式或固定比例计算单笔投入金额（基于 $1,000 起始资金）。
2. **滑点预估**: 检查 CLOB 深度，预估交易对价格的影响。
3. **合规锁死**: 检查是否触发单日最大回撤或单市场头寸上限。

## 阶段 4：极速执行 (Execution)
**[执行主体: Execution Agent]**
1. **签名生成**: 后端自动处理 EIP-712 结构化签名。
2. **订单分发**: 通过 Cloudflare Workers 将订单极速推送到 Polymarket 节点。
3. **状态回执**: 监听成交情况，实时反馈至看板。

## 阶段 5：动态管理 (Management)
**[执行主体: Monitoring System]**
1. **实时 PnL**: 毫秒级刷新当前持仓的浮动盈亏。
2. **条件触发**: 预设止盈/止损价格，或当逻辑条件消失时自动平仓。

## 6. 审计与复盘 (Audit & Replay)
**[执行主体: Audit Engine]**
1. **决策链存证**: 将从情报到执行的完整决策链条存入 D1 数据库。
2. **绩效归因**: 分析该笔交易的利润来源（是由于判断准确，还是由于对手方失误）。
3. **策略迭代**: 根据审计结果，自动调整 Intelligence 或 Logic 层的参数。

---

## 系统工作流循环 (Loop)
`感知 (P) -> 思考 (T) -> 决策 (D) -> 执行 (A) -> 记录 (R)`
P30 操作系统将 24/7 不间断地运行此循环，实现资产特增值。
